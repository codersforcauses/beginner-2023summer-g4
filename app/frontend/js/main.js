/*
fetch(`${endpoint}/api/example`)
    .then(res => res.json())    
    .then(data => {
        document.getElementById('example').innerText = JSON.stringify(data, null, 2);
    })
*/
    // <p id="example"></p>